import { LayoutContextType } from "@/types";

export const LAYOUT_DEFAULT: LayoutContextType = {
  device: "desktop",
  width: 0,
  height: 0,
};

export const LAYOUT_THRESHOLD_DESKTOP = 1024;
export const LAYOUT_THRESHOLD_TABLET = 768;
